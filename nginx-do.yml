---
- hosts: nginx_do
  become: yes
  roles:
    - common-internet-v3
    - docker-install
    - docker-configure-firewall-v2
    - docker-compose
    - nginx-base-config
    - nginx-docker-prefly
    - nginx-cert-selfsign
    - nginx-docker-install
    - nginx-vhosts-configure
    - ufw-configure
    - ufw-management
