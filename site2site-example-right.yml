---
- hosts: site2site_right
  become: yes
  vars_prompt:
    - name: vault_login
      prompt: "What is your LDAP  login ?"
      private: no
    - name: vault_password
      prompt: "What is your LDAP password ?"
  roles:
    - net-interface-check
    - common-v2
    - zabbix-agent # optionality
    - node-exporter # optionality
    - vault-get-token
    - vault-get-users-secrets-v2
    - strongswan-install
    - firewall-configure
    - ufw-general