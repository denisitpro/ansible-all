---
- name: Ensure graylog-dump directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
    owner: root
    group: root
  loop:
    - "{{ graylog_exports_install_dir }}"
    - "{{ graylog_exports_output_dir }}"