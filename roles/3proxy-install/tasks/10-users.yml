---
- name: "Create user list file"
  ansible.builtin.template:
    src: "users.cfg.j2"
    dest: "{{ proxy_conf_dir }}/users.cfg"
    owner: root
    group: root
    mode: 0644
  become: true
  notify:
    - "Restart docker-compose 3proxy"
