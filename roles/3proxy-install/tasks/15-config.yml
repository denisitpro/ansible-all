---
- name: "Create config for 3proxy"
  ansible.builtin.template:
    src: "3proxy.cfg.j2"
    dest: "{{ proxy_conf_dir }}/3proxy.cfg"
    owner: root
    group: root
    mode: 0644
  become: true
  notify:
    - "Restart docker-compose 3proxy"
