---
- name: "Update apt cache"
  apt:
    update_cache: yes
  changed_when: false

- name: install longhorn depedndens
  ansible.builtin.apt:
    name: "{{ longhorn_packages }}"
    state: latest
    update_cache: yes
    cache_valid_time: 3600
