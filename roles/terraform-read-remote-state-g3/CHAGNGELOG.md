# g3
Added support for filtering Terraform resources by labels. Creates list variables with all instances matching specified label key-value pairs.

# g2
Fixed proper sorting order for IP addresses when extracting resources with count/array elements.
