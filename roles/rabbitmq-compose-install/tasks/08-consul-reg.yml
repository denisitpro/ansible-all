---
- name: Copy rabbitmq  consul config
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/consul.d/{{ item }}"
    mode: 0644
  loop:
    - "rabbitmq_exporter.hcl"
    - "rabbitmq.hcl"
  notify: restart-docker folder consul
