---
- name: stop outline docker compose project
  community.docker.docker_compose_v2:
    project_src: "{{ outline_compose_path }}"
    state: absent
  ignore_errors: true # noqa: ignore-errors
  tags: ['never', 'outline-clean']
