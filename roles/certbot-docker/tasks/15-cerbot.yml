---
- name: copy certbot
  template:
    src: "docker-compose.certbot.j2"
    dest: "{{docker_compose_path}}/certbot/docker-compose.yml"
    owner: root
    group: root
    mode: 0644

- name: run restart compose
  shell: "docker-compose up -d"
  args:
    chdir: "{{docker_compose_path}}/certbot"