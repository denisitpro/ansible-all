{{ ansible_managed }}
127.0.0.1 localhost	localhost.localdomain {% if server_localhost_force is defined %} {{ aws_server_name.stdout }} {{ aws_server_name.stdout }}.{{aws_fqdn_domain}}{% endif %}

{% if ipa_container_local is defined %}{{freeipa_bind_addr | default(global_bind_addr)}} {{ ipa_host_header | default(aws_server_name.stdout.aws_fqdn_domain) }}{% endif %}

# The following lines are desirable for IPv6 capable hosts
::1 ip6-localhost ip6-loopback
fe00::0 ip6-localnet
ff00::0 ip6-mcastprefix
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
ff02::3 ip6-allhosts