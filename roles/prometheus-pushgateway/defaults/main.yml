---
pushgateway_user: pushgateway
pushgateway_group: "{{pushgateway_user}}"

pushgateway_version: 1.0.1
pushgateway_web_listen_address: "{{global_bind_addr}}:9091"
pushgateway_web_external_url: ""

pushgateway_persistence: true
pushgateway_persistence_dir: /opt/pushgateway


pushgateway_config_flags_extra: {}
# pushgateway_config_flags_extra:
#   persistence.interval: '5m'