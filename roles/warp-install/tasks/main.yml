---
- ansible.builtin.import_tasks: 05-install.yml
  tags:
    - warp
    - install
  when: ansible_distribution == "Ubuntu"

# - ansible.builtin.import_tasks: 10-configure.yml
#   tags:
#     - warp
#     - configure
#   when: ansible_distribution == "Ubuntu" and warp_team_name != ""
