---
clickhouse_version: 22.3.11.12
clickhouse_bind_addr: "{{global_bind_addr}}"
clickhouse_port: 8123

clickhouse_path: /opt/clickhouse
clickhouse_exporter_version: latest
clickhouse_exporter_port: 9116

exporter_user: user_ch_exporter
max_concurrent_queries: 100
max_parts_in_total: 100000
parts_to_throw_insert: 300


clickhouse_listen_host: "{{global_bind_addr}}"

docker_compose_app_path: "{{docker_compose_path}}"
