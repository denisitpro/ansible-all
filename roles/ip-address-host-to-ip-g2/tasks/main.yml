---
- ansible.builtin.import_tasks: 05-resolv.yml
  delegate_to: "{{ ip_hosts_init_host }}"
  when: list_servers_resolved_g2 | default([]) | length > 0
  run_once: true
  tags:
    - always
    - ip-resolve
    - ip-detect
