---
- name: install requirements exchange_endpoint - centos 8
  yum:
    name: "{{ item.name }}"
    state: "{{ item.state }}"
    update_cache: yes
  with_items:
    - { name: "jq", state: "latest" }
  when:
    - zabbix.requirements is defined
    - '"exchange_endpoint" in zabbix.requirements'


