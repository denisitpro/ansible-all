---
- name: copy dymanic version checker script
  ansible.builtin.template:
    src: dynamic-docker-version-checker.sh.j2
    dest: "{{ version_scripts_path }}/dynamic-docker-version-checker.sh"
    mode: "0700"
    owner: root
    group: root
