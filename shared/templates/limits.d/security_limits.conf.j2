## Example hard limit for max opened files
*   hard nofile {{hard_nofile | default (700000) }}
*   hard nproc {{hard_nproc| default (700000) }}

## Example soft limit for max opened files
*   soft nofile {{soft_nofile | default (700000) }}
*   soft nproc {{soft_nproc| default (700000) }}
