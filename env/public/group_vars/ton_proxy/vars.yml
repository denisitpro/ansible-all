---
#tonproxy_restart: true

#tonproxy_image_version: debug1

iptables:
  ipv4:
# mgmt access
    - { name: "ssh_any_access", protocol: "tcp", port: "22"}
# app access
    - { name: "ton-proxy-access", protocol: "tcp", port: "8080"}
    - { name: "ton-blockchain-access", protocol: "udp", port: "3333"}
  ipv6:
# mgmt access
    - { name: "ssh_any_access", protocol: "tcp", port: "22"}
# app access
    - { name: "ton-proxy-access", protocol: "tcp", port: "8080"}
    - { name: "ton-blockchain-access", protocol: "udp", port: "3333"}